# Phase 04.5 — UI Review

Audited: 2026-04-27
Rechecked: 2026-04-27T01:12:18Z
Baseline: `04.5-STYLE-CONTRACT.md`, `04.5-CONTEXT.md`, Phase 04 UI-SPEC, Phase 04.5 PLAN/SUMMARY artifacts
Screenshots: not captured; no local dev server was detected on common Vite ports, so this is a code-only audit
Recheck verdict: PARTIAL PASS — top-priority fixes are closed, but two minor UI contract items remain

---

## RECHECK 2026-04-27 — CHANGE LOG

This section is intentionally prominent so the recheck delta is easy to see.

| Prior Item | Recheck Status | Evidence |
|------------|----------------|----------|
| Dashboard row clicks forced a full reload | CLOSED | `web/src/routes/index.tsx:20`, `web/src/routes/index.tsx:35`, and `web/src/routes/index.tsx:145` now use TanStack Router navigation. |
| Financial status colors used generic `--accent-*` tokens | CLOSED | `web/src/lib/variants.ts:55`, `web/src/lib/variants.ts:96`, `web/src/lib/variants.ts:123`, `web/src/lib/variants.ts:161`, and `web/src/lib/variants.ts:182` now use signal, score, or risk tokens. |
| Daily change used generic success/error accent colors | CLOSED | `web/src/components/views/ScreeningResultItem.tsx:74` and `web/src/components/views/ScreeningResultItem.tsx:75` now use `--signal-positive` and `--signal-negative`. |
| Dossier evidence components used raw card-like div surfaces | CLOSED | `web/src/components/views/ChartContainer.tsx:182`, `web/src/components/views/ChartContainer.tsx:194`, `web/src/components/views/HoldingsList.tsx:24`, and `web/src/components/views/HoldingsList.tsx:39` now use `Surface`. |
| Metric values used stronger `font-bold` emphasis | CLOSED | `web/src/components/views/StatsCard.tsx:127` and `web/src/routes/funds/$code.tsx:134` now use `font-semibold`. |
| Chart query error is not surfaced independently | OPEN | `web/src/routes/funds/$code.tsx:41` still reads only `data` and `isLoading` from `useChartData`; no chart error copy/toast is shown. |
| `/funds` archive still nests `ArchiveTable` around a `FundTable` Surface | OPEN | `web/src/routes/funds/index.tsx:67` wraps `FundTable`, while `web/src/components/views/FundTable.tsx:47` creates another `Surface`. |

Validation run:

- `bun run lint` in `web` passed.
- `bun run build` in `web` passed.

---

## Pillar Scores

| Pillar | Score | Recheck Finding |
|--------|-------|-----------------|
| 1. Copywriting | 4/4 | Domain-specific Chinese page copy, loading states, empty states, and error toasts are present. |
| 2. Visuals | 3/4 | Dossier evidence surfaces are fixed; `/funds` still has a double archive surface. |
| 3. Color | 4/4 | Financial state colors now use signal, score, or risk tokens; no forbidden Tailwind color classes were found in the rechecked scope. |
| 4. Typography | 4/4 | The previous `font-bold` metric emphasis is now `font-semibold`, matching the restrained contract. |
| 5. Spacing | 3/4 | Spacing remains consistent; table arbitrary column widths are still localized in `FundTable`. |
| 6. Experience Design | 3/4 | Router navigation is fixed; chart query errors are still not surfaced independently. |

Overall: 21/24

---

## Remaining Priority Fixes

1. Add chart-specific error feedback — users can distinguish chart evidence failure from no chart data — read `error` from `useChartData` in `web/src/routes/funds/$code.tsx:41` and show either a toast or a local evidence-panel error state.
2. Remove the `/funds` double surface — the Archive List page has one clean archive container — either let `ArchiveTable` own the surface or add a surface-free table rendering mode to `FundTable`.

---

## Detailed Findings

### Pillar 1: Copywriting (4/4)

Passing evidence:

- `web/src/routes/index.tsx:68` uses a clear product eyebrow, `FUND SCREENER`.
- `web/src/routes/index.tsx:69` and `web/src/routes/index.tsx:70` explain the dashboard purpose in user-facing language.
- `web/src/routes/funds/index.tsx:61` through `web/src/routes/funds/index.tsx:64` frame `/funds` as a fund archive instead of a generic admin list.
- `web/src/routes/funds/$code.tsx:65` through `web/src/routes/funds/$code.tsx:68` derive a natural-language holdings summary when data exists and provide a useful no-holdings sentence when it does not.
- `web/src/routes/screening/index.tsx:96` and `web/src/routes/index.tsx:136` provide explicit empty-state copy instead of blank panels.

No copywriting issue remains.

### Pillar 2: Visuals (3/4)

Passing evidence:

- `web/src/routes/index.tsx:66` and `web/src/routes/index.tsx:67` use `PageShell` and `PageHeader` for the Research Dashboard archetype.
- `web/src/routes/funds/index.tsx:59` through `web/src/routes/funds/index.tsx:68` use `PageShell`, `PageHeader`, and `ArchiveTable` for the Archive List archetype.
- `web/src/routes/funds/$code.tsx:106` through `web/src/routes/funds/$code.tsx:196` follow the Dossier flow: header, diagnosis grid, NAV evidence, and holdings evidence.
- `web/src/components/views/ChartContainer.tsx:182` and `web/src/components/views/HoldingsList.tsx:39` now use `Surface` instead of raw card-like containers.

Remaining finding:

- `web/src/routes/funds/index.tsx:67` wraps `FundTable` in `ArchiveTable`, while `web/src/components/views/FundTable.tsx:47` creates its own `Surface`. This still creates a nested archive card and weakens the single ArchiveSurface contract.

Recommended fix: let only one component own the archive surface. The smaller change is to add a `surface={false}` or `unstyled` mode to `FundTable` when it is rendered inside `ArchiveTable`.

### Pillar 3: Color (4/4)

Passing evidence:

- No rechecked runtime matches were found for forbidden classes such as `bg-white`, `text-gray-*`, `text-red-*`, `text-green-*`, `shadow-lg`, or arbitrary gradients.
- `web/src/styles/tokens.semantic.css:53` through `web/src/styles/tokens.semantic.css:78` define signal, exposure, score, and risk tokens.
- `web/src/lib/variants.ts:55` through `web/src/lib/variants.ts:61` use score/signal tokens for score levels.
- `web/src/lib/variants.ts:96` through `web/src/lib/variants.ts:100` use signal tokens for purchase status.
- `web/src/lib/variants.ts:123` through `web/src/lib/variants.ts:129` use signal tokens for MA strength.
- `web/src/lib/variants.ts:182` through `web/src/lib/variants.ts:184` use risk tokens for volatility.
- `web/src/components/views/ScreeningResultItem.tsx:74` and `web/src/components/views/ScreeningResultItem.tsx:75` use signal tokens for daily change.

No color issue remains in the rechecked scope.

### Pillar 4: Typography (4/4)

Passing evidence:

- Scoped files primarily use `text-xs`, `text-sm`, `text-lg`, and `text-2xl`, matching the restrained Phase 4 UI-SPEC scale.
- `web/src/components/ui/page-header.tsx:30` uses `text-2xl font-semibold tracking-tight`, matching the page title rule.
- `web/src/components/views/StatsCard.tsx:127` and `web/src/routes/funds/$code.tsx:134` now use `font-semibold` instead of `font-bold`.
- Numeric components use mono/tabular rhythm in `web/src/components/ui/metric-value.tsx:20`, `web/src/components/views/HoldingsList.tsx:67`, and `web/src/components/views/HoldingsList.tsx:71`.

No typography issue remains in the rechecked scope.

### Pillar 5: Spacing (3/4)

Passing evidence:

- Page shells use consistent responsive padding in `web/src/components/ui/page-shell.tsx:12`.
- Phase routes mostly use `gap-3`, `gap-4`, `gap-6`, `space-y-3`, and `space-y-4`, staying within the intended calm spacing system.
- Archive rows are constrained through `web/src/components/ui/archive-table.tsx:14`, which sets table cell heights for dense archive rhythm.

Remaining finding:

- `web/src/components/views/FundTable.tsx:52`, `web/src/components/views/FundTable.tsx:54`, `web/src/components/views/FundTable.tsx:55`, and `web/src/components/views/FundTable.tsx:68` still use arbitrary width classes for table columns. They are localized and not a blocker, but they should be promoted into table primitives if more archive tables are added.

### Pillar 6: Experience Design (3/4)

Passing evidence:

- Loading states exist for dashboard, funds list, screening, and fund dossier: `web/src/routes/index.tsx:51`, `web/src/routes/funds/index.tsx:47`, `web/src/routes/screening/index.tsx:52`, and `web/src/routes/funds/$code.tsx:81`.
- Error feedback uses toast notifications in `web/src/routes/index.tsx:40`, `web/src/routes/funds/index.tsx:40`, `web/src/routes/screening/index.tsx:45`, and `web/src/routes/funds/$code.tsx:48`.
- Empty states are present in `web/src/routes/index.tsx:134`, `web/src/routes/screening/index.tsx:95`, and `web/src/components/views/HoldingsList.tsx:21`.
- Dashboard top-candidate navigation now uses TanStack Router through `web/src/routes/index.tsx:145`.

Remaining finding:

- `web/src/routes/funds/$code.tsx:41` does not read `error` from `useChartData`. If chart evidence fails while fund detail succeeds, the current UI falls through to empty chart data instead of a chart-specific failure message.

Recommended fix: read `error: chartError` from `useChartData`, show a toast in the existing error effect, and pass an explicit chart evidence error state to the chart panel.

---

## Screenshot and Safety Notes

- `.planning/ui-reviews/.gitignore` exists before screenshot detection.
- No local dev server was detected on ports `3000`, `4173`, `5173`, or `5174` during the first audit pass.
- Playwright-MCP tools were not available in this Codex session, so screenshots were skipped and this audit used code evidence only.
- Registry safety audit was not applicable: `components.json` exists, but the Phase UI specs do not list third-party registry additions for this phase.

---

## Files Audited

- `.planning/phases/04.5-style-contract/04.5-01-PLAN.md`
- `.planning/phases/04.5-style-contract/04.5-01-SUMMARY.md`
- `.planning/phases/04.5-style-contract/04.5-02-PLAN.md`
- `.planning/phases/04.5-style-contract/04.5-02-SUMMARY.md`
- `.planning/phases/04.5-style-contract/04.5-CONTEXT.md`
- `.planning/phases/04.5-style-contract/04.5-STYLE-CONTRACT.md`
- `.planning/phases/04-ui-renovation/04-UI-SPEC.md`
- `.claude/skills/claude-ui-system/SKILL.md`
- `.claude/skills/claude-ui-system/anti-patterns.md`
- `.claude/skills/claude-ui-system/checklist.md`
- `.claude/skills/claude-ui-system/patterns/page-archetypes.md`
- `web/src/components/ui/archive-table.tsx`
- `web/src/components/ui/metric-value.tsx`
- `web/src/components/ui/page-header.tsx`
- `web/src/components/ui/page-shell.tsx`
- `web/src/components/ui/signal-badge.tsx`
- `web/src/lib/variants.ts`
- `web/src/routes/funds/$code.tsx`
- `web/src/routes/funds/index.tsx`
- `web/src/routes/index.tsx`
- `web/src/routes/screening/index.tsx`
- `web/src/styles/tokens.semantic.css`
- `web/src/components/views/ChartContainer.tsx`
- `web/src/components/views/FundTable.tsx`
- `web/src/components/views/HoldingsList.tsx`
- `web/src/components/views/ScreeningResultItem.tsx`
- `web/src/components/views/StatsCard.tsx`
