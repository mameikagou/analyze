---
phase: 04.5-style-contract
plan: 01
subsystem: ui
tags: [react, tailwind, design-system, claude-skill, semantic-tokens]
requires:
  - phase: 04-ui-renovation
    provides: Stone/Semantic/Surface visual system, dark mode, animation and component baseline
provides:
  - Financial semantic token layer for signals, exposure, scores, and risk
  - Page composition primitives for AI-generated archive-style pages
  - Project-local Claude UI generation skill with archetypes and guardrails
  - Historical Phase 4 token guidance corrected away from primitive token examples
  - Screening page proof that Archive List contract can be adopted incrementally
affects: [frontend-ui, page-generation, screening-page, design-system]
tech-stack:
  added: []
  patterns:
    - Surface-composed page primitives
    - Archive finance list layout
    - Semantic financial status tokens
key-files:
  created:
    - web/src/components/ui/page-shell.tsx
    - web/src/components/ui/page-header.tsx
    - web/src/components/ui/metric-value.tsx
    - web/src/components/ui/signal-badge.tsx
    - web/src/components/ui/archive-table.tsx
    - .claude/skills/claude-ui-system/SKILL.md
    - .claude/skills/claude-ui-system/patterns/page-archetypes.md
    - .claude/skills/claude-ui-system/anti-patterns.md
    - .claude/skills/claude-ui-system/checklist.md
  modified:
    - web/src/styles/tokens.semantic.css
    - web/src/routes/screening/index.tsx
    - .planning/phases/04-ui-renovation/04-02-PLAN.md
key-decisions:
  - "Kept new primitives in web/src/components/ui to satisfy the Phase 4.5 contract while composing through the existing design-system/Surface component."
  - "Used signal tokens for financial status and left raw exposure hex values confined to tokens.semantic.css."
  - "Applied the contract to /screening only as a proof, avoiding a full page rewrite outside the plan scope."
patterns-established:
  - "Generated pages must start with an archetype and compose through PageShell/PageHeader/Surface-derived primitives."
  - "Financial color is semantic: components use --signal-* / --score-* / --risk-* rather than primitive color tokens."
requirements-completed: [DSGN-01, DSGN-02, DSGN-03, DSGN-05, DSGN-07, DSGN-08, FRONT-01, FRONT-02, FRONT-03, FRONT-05]
duration: 60min
completed: 2026-04-26
---

# Phase 04.5: Style Contract and AI Generation Guardrails Summary

**Claude-like fund archive UI contract made executable through financial tokens, page primitives, local AI skill rules, and a screening-page adoption proof.**

## Performance

- **Duration:** ~60 min
- **Started:** 2026-04-26T22:17:00+08:00
- **Completed:** 2026-04-26T23:17:00+08:00
- **Tasks:** 5 completed
- **Files modified:** 12 planned deliverable files plus this summary

## Accomplishments

- Added signal, exposure, score, and risk semantic tokens in `tokens.semantic.css`, including dark-mode subtle signal backgrounds.
- Created `PageShell`, `PageHeader`, `MetricValue`, `SignalBadge`, and `ArchiveTable` primitives that reuse the existing `Surface` system.
- Added `.claude/skills/claude-ui-system/` with archetypes, forbidden patterns, and generation/visual checklists.
- Corrected Phase 4 historical plan examples from primitive `--green-500` / `--red-500` usage to `--signal-positive` / `--signal-negative`.
- Converted `/screening` to use PageShell, PageHeader, and ArchiveTable as the first real Archive List adoption proof.

## Task Commits

Each planned task was committed atomically:

1. **Task 1: Add financial semantic tokens** — `1da7058` (feat)
2. **Task 2: Create page composition primitives** — `302c5d0` (feat)
3. **Task 3: Write project-local Claude UI skill rules** — `5810ab6` (feat)
4. **Task 4: Fix historical plan token contradiction** — `8270103` (docs)
5. **Task 5: Validate one real page against the contract** — `e6fdc0e` (feat)

**Correction commit:** `c574175` removed unrelated pre-staged local artifacts from tracking after they were accidentally included in `1da7058`.

## Files Created/Modified

- `web/src/styles/tokens.semantic.css` — Adds financial semantic token layer.
- `web/src/components/ui/page-shell.tsx` — Route-level canvas wrapper using `Surface`.
- `web/src/components/ui/page-header.tsx` — Standard page heading and status summary primitive.
- `web/src/components/ui/metric-value.tsx` — Mono/tabular metric renderer with signal tone mapping.
- `web/src/components/ui/signal-badge.tsx` — Signal badge mapped to `--signal-*` tokens.
- `web/src/components/ui/archive-table.tsx` — ArchiveSurface wrapper for dense evidence lists/tables.
- `.claude/skills/claude-ui-system/SKILL.md` — Local skill entrypoint for future page generation.
- `.claude/skills/claude-ui-system/patterns/page-archetypes.md` — Research Dashboard, Archive List, Fund Dossier, and Chat/Composer archetypes.
- `.claude/skills/claude-ui-system/anti-patterns.md` — Forbidden utility classes and structural bans.
- `.claude/skills/claude-ui-system/checklist.md` — AI generation and visual acceptance checklists.
- `.planning/phases/04-ui-renovation/04-02-PLAN.md` — Corrected primitive token examples.
- `web/src/routes/screening/index.tsx` — Archive List proof using PageShell, PageHeader, and ArchiveTable.

## Decisions Made

- Reused `design-system/Surface` inside `web/src/components/ui` primitives rather than creating a parallel container system.
- Kept the `/screening` adoption intentionally narrow: layout contract proof only, no full redesign or data-flow change.
- Treated existing forbidden-class hits inside design-system comments as documentation examples, not runtime violations.

## Deviations from Plan

### Auto-fixed Issues

**1. Commit hygiene correction for unrelated staged artifacts**
- **Found during:** Task 1 commit
- **Issue:** `.planning/v1.0-MILESTONE-AUDIT.md` and `web/src/design/博主列表.html` were already staged before Phase 4.5 execution and were included in `1da7058`.
- **Fix:** Committed `c574175` to remove those paths from git tracking while leaving local untracked files intact.
- **Files modified:** `.planning/v1.0-MILESTONE-AUDIT.md`, `web/src/design/博主列表.html`
- **Verification:** `git status --short` shows both paths as untracked local files again.
- **Committed in:** `c574175`

---

**Total deviations:** 1 auto-fixed commit hygiene issue
**Impact on plan:** No product or implementation scope change; repository state was corrected without deleting local user artifacts.

## Issues Encountered

- `gsd-executor` subagent failed before tool use with a temporary runtime 503, so execution continued inline under the execute-phase fallback.
- The initial grep command was run from `web/` after `bun` changed shell working directory, so it was rerun with absolute paths.

## Verification

- `cd web && bun run lint` — passed.
- `cd web && bun run build` — passed.
- Forbidden Tailwind grep — only matched existing comment examples in `Surface.tsx` and `design-system/index.ts`; no runtime component or route usage was introduced.
- Primitive token grep for `var(--green-500)` / `var(--red-500)` across components, routes, and `04-02-PLAN.md` — no output.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

Future page rewrites can now start from the local `claude-ui-system` skill and select an archetype before generating code. The `/screening` page proves the Archive List contract can be adopted incrementally without changing API hooks or routing.

---
*Phase: 04.5-style-contract*
*Completed: 2026-04-26*
