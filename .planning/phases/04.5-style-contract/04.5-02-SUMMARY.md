---
phase: 04.5
plan: 02
name: Kimi-executable Page Renovation Queue
status: complete
completed: "2026-04-26"
---

# 04.5-02 执行摘要

## 完成内容

| 任务 | 页面 | Archetype | 状态 |
|------|------|-----------|------|
| 1 | `/funds` | Archive List Page | ✅ 完成 |
| 2 | `/funds/$code` | Fund Dossier v0.5 | ✅ 完成 |
| 3 | `/` | Research Dashboard Page | ✅ 完成 |
| 4 | Forbidden-pattern cleanup | 全站 runtime 扫描 | ✅ 完成 |
| 5 | Build + handoff | lint + build + SUMMARY | ✅ 完成 |

## 具体改动

### Task 1: /funds → Archive List Page
- **文件**: `web/src/routes/funds/index.tsx`
- **改动**: 替换裸写标题块为 `PageShell` + `PageHeader`（eyebrow: FUND ARCHIVE, title: 基金档案馆）
- `FundTable` 包在 `ArchiveTable` 内，统一档案列表容器
- 保持数据流不变：`useFunds` hook、分页参数、跳转行为

### Task 2: /funds/$code → Fund Dossier v0.5
- **文件**: `web/src/routes/funds/$code.tsx`
- **改动**:
  - 使用 `PageShell` + `PageHeader` 作为 DossierHeader（identity: code + name + market）
  - 新增 `DiagnosisGrid`：4 张诊断卡片（持仓集中度 Top3、重仓股数量、基金规模、成立日期）
  - 新增自然语言摘要："前三大重仓为 X、Y、Z，合计占比 XX%。"
  - `EvidenceLayout`：左列净值走势（`ChartContainer`），右列持仓证据（`HoldingsList`）
  - 所有卡片走 `Surface` 容器，数字走 `MetricValue`，状态走 `SignalBadge`
- 保持数据流不变：`useFundDetail` + `useChartData`、not-found 行为、back navigation
- **未实现**（按 plan 要求）: force graph、timeline、serif font、新 API 调用

### Task 3: / → Research Dashboard Page
- **文件**: `web/src/routes/index.tsx`
- **改动**:
  - 使用 `PageShell` + `PageHeader`（eyebrow: FUND SCREENER, title: 全市场趋势筛选）
  - `StatsCard` 行作为 InsightStrip 保留
  - ScreeningResultItem 列表包在 `Surface` 容器内
  - PageHeader actions 添加"查看全部"链接到 `/screening`
- 保持数据流不变：`useStats` + `useScreening`

### Task 4: Forbidden-pattern cleanup
- **文件**: `web/src/lib/variants.ts`
- **改动**: 将所有 primitive token 替换为 semantic token
  - `var(--red-100)` / `var(--red-600)` → `var(--accent-error-subtle)` / `var(--accent-error)`
  - `var(--green-100)` / `var(--green-600)` → `var(--accent-success-subtle)` / `var(--accent-success)`
  - `var(--blue-100)` / `var(--blue-600)` → `var(--accent-info-subtle)` / `var(--accent-info)`
  - `var(--orange-100)` / `var(--orange-600)` → `var(--accent-primary-subtle)` / `var(--accent-primary)`
  - `var(--stone-200)` / `var(--stone-500)` → `var(--bg-active)` / `var(--text-muted)`
- **扫描结果**: `src/routes` 和 `src/components` 运行时代码中零禁用模式（`bg-white` / `text-gray-*` / `text-red-*` / `text-green-*` / `shadow-lg` / `shadow-xl`）
- **扫描结果**: 零 primitive token（`var(--green-500)` / `var(--red-500)`）在运行时代码中

### Task 5: Build verification
- `bun run lint` → exit 0 ✅
- `bun run build` → exit 0 ✅（481ms，无 error）

## 提交记录

| Commit | 说明 |
|--------|------|
| `67f6c3d` | feat(04.5-02): refactor /funds to Archive List Page archetype |
| `a911512` | refactor(04.5-02): replace primitive tokens with semantic tokens in variants.ts |
| `f56fe05` | feat(04.5-02): refactor /funds/$code to Fund Dossier v0.5 archetype |
| `e61cda4` | feat(04.5-02): refactor home page to Research Dashboard Page archetype |

## 遗留事项 / 视觉后续

- Milestone v1.0 completion was not evaluated.
- `/chat` 页面未在本次翻新范围内（非高优先级）。
- `/backtest` 页面已在 Phase 4 翻新，本次未动。

## 验收清单

- [x] `/funds` follows Archive List Page archetype
- [x] `/funds/$code` follows Fund Dossier Page archetype
- [x] `/` follows Research Dashboard Page archetype
- [x] Modified pages use Phase 4.5 primitives (PageShell, PageHeader, Surface, ArchiveTable, MetricValue, SignalBadge)
- [x] Modified pages do not contain forbidden hardcoded visual classes
- [x] No backend, routing, API hook, GSD status, milestone audit, or product-direction changes
- [x] Lint passes
- [x] Build passes
