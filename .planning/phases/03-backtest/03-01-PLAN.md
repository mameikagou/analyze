---
phase: 03-backtest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fund_screener/factors/__init__.py
  - src/fund_screener/factors/base.py
  - src/fund_screener/factors/technical.py
  - src/fund_screener/factors/quant.py
  - src/fund_screener/factors/composite.py
  - src/fund_screener/storage.py
  - tests/test_factors.py
  - pyproject.toml
autonomous: true
requirements:
  - BACK-01
  - BACK-02
must_haves:
  truths:
    - "All factors accept nav_panel (date x fund_code DataFrame) and output FactorOutput with consistent shape"
    - "MACrossFactor outputs boolean matrix with first `long` days forced False and NaN positions as False"
    - "CompositeFactor performs cross-sectional Z-Score normalization per date row"
    - "storage.load_nav_panel() returns wide-format DataFrame with ffill(limit=5) applied"
    - "Each factor has at least 2 test cases (normal + boundary)"
  artifacts:
    - path: "src/fund_screener/factors/base.py"
      provides: "BaseFactor abstract class + FactorOutput dataclass"
      exports: ["BaseFactor", "FactorOutput"]
    - path: "src/fund_screener/factors/technical.py"
      provides: "MACrossFactor"
      exports: ["MACrossFactor"]
    - path: "src/fund_screener/factors/quant.py"
      provides: "MomentumFactor, SharpeFactor, MaxDrawdownFactor"
      exports: ["MomentumFactor", "SharpeFactor", "MaxDrawdownFactor"]
    - path: "src/fund_screener/factors/composite.py"
      provides: "CompositeFactor with Z-Score normalization"
      exports: ["CompositeFactor"]
    - path: "src/fund_screener/factors/__init__.py"
      provides: "Package barrel export"
      exports: ["BaseFactor", "FactorOutput", "MACrossFactor", "MomentumFactor", "SharpeFactor", "MaxDrawdownFactor", "CompositeFactor"]
    - path: "src/fund_screener/storage.py"
      provides: "load_nav_panel() method added to DataStore"
      contains: "def load_nav_panel"
    - path: "tests/test_factors.py"
      provides: "Factor unit tests"
      min_tests: 12
  key_links:
    - from: "factors/base.py"
      to: "factors/technical.py, factors/quant.py, factors/composite.py"
      via: "inheritance from BaseFactor"
      pattern: "class .*Factor\(BaseFactor\)"
    - from: "factors/composite.py"
      to: "factors/quant.py"
      via: "CompositeFactor accepts list of BaseFactor instances"
      pattern: "factors: List\[BaseFactor\]"
    - from: "storage.py"
      to: "factors/*.py"
      via: "load_nav_panel() output feeds into factor.compute(nav_panel)"
      pattern: "nav_panel: pd\.DataFrame"
---

<objective>
Build the factor layer abstraction and data loading infrastructure for the backtest engine.

Purpose: Establish the factor/strategy decoupling architecture per ARCHITECTURE.md principle #1. All factors consume the same nav_panel format and produce the same FactorOutput contract, enabling plug-and-play factor combinations.
Output: 5 factor classes + load_nav_panel() + comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHITECTURE.md
@.planning/BACKTEST_DESIGN.md
@src/fund_screener/storage.py
@src/fund_screener/risk_metrics.py
@src/fund_screener/screener.py

## Locked Decisions (from 03-CONTEXT.md)

- **D-01: vectorbt v1** for backtest engine — implemented in Plan 02
- **D-02: nav_panel NaN = ffill(limit=5) + False for signal positions** — implemented in this plan
- **D-04: New factors/ package, existing screener.py/scoring.py untouched** — per this plan's file list
- **D-05: No kind='weight' in Phase 3, interface reserved** — FactorOutput.kind Literal only includes "signal" | "score" | "weight"

## Existing Code Patterns (from codebase)

From storage.py DataStore class:
- Uses `sqlite3.connect(..., check_same_thread=False)`
- Uses `pd.read_sql_query()` for DataFrame loading
- Uses `self._conn.execute()` for raw SQL
- All persist methods wrap in try/except with logger.warning on failure

From risk_metrics.py:
- Pure functions: input pd.Series, output float
- Boundary defense: data insufficient / zero volatility / empty series -> return float('nan')
- Uses `math.sqrt()` not `np.sqrt()` for scalar operations
- Uses `std(ddof=1)` for sample standard deviation

From screener.py:
- `calculate_ma()` uses `rolling(window=period, min_periods=period).mean()`
- `screen_fund()` returns `ScreenResult | None`

## Interface Contracts

```python
# FactorOutput (from BACKTEST_DESIGN.md §3.2)
@dataclass(frozen=True)
class FactorOutput:
    values: pd.DataFrame          # shape == nav_panel.shape
    kind: Literal["signal", "score", "weight"]
    name: str
    description: str = ""

# BaseFactor.compute signature
@abstractmethod
def compute(self, nav_panel: pd.DataFrame, **context) -> FactorOutput:
    ...
```

## vectorbt Dependency

Add to pyproject.toml dependencies: `"vectorbt==0.26.0"` (pinned per BACKTEST_DESIGN.md §6.5)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create factor layer package (base + 4 factor implementations)</name>
  <files>
    src/fund_screener/factors/__init__.py
    src/fund_screener/factors/base.py
    src/fund_screener/factors/technical.py
    src/fund_screener/factors/quant.py
    src/fund_screener/factors/composite.py
  </files>
  <read_first>
    src/fund_screener/screener.py
    src/fund_screener/risk_metrics.py
    .planning/BACKTEST_DESIGN.md
    .planning/ARCHITECTURE.md
  </read_first>
  <action>
    Create the factors/ package with these exact implementations per BACKTEST_DESIGN.md §3-4:

    **1. factors/base.py:**
    - Define `FactorOutput` dataclass(frozen=True) with fields: values(pd.DataFrame), kind(Literal["signal","score","weight"]), name(str), description(str="")
    - Define abstract `BaseFactor` class with `compute(self, nav_panel: pd.DataFrame, **context) -> FactorOutput` method
    - Add `__add__` method that returns `CompositeFactor` (import from .composite lazily to avoid circular import)
    - Add docstring referencing ARCHITECTURE.md principle #2

    **2. factors/technical.py:**
    - `MACrossFactor(BaseFactor)` with `__init__(self, short: int = 20, long: int = 60)`
    - `compute()` does panel-level rolling mean comparison:
      - `fast = nav_panel.rolling(window=self.short, min_periods=self.short).mean()`
      - `slow = nav_panel.rolling(window=self.long, min_periods=self.long).mean()`
      - `signal = fast > slow`
      - `signal.iloc[:self.long] = False` (force first `long` days to False)
      - `signal = signal.where(nav_panel.notna(), False)` (NaN positions -> False)
    - Returns FactorOutput with kind="signal"

    **3. factors/quant.py:**
    - `MomentumFactor(BaseFactor)`: `(nav - MA20) / MA20`, kind="score"
    - `SharpeFactor(BaseFactor)`: rolling annualized Sharpe with lookback=252, rf=0.02. Use `returns.pct_change()`, excess = returns - rf/252, rolling_mean / rolling_std * sqrt(252). Use `math.sqrt(252)` for scalar (consistent with risk_metrics.py). Handle std==0 -> NaN.
    - `MaxDrawdownFactor(BaseFactor)`: rolling max drawdown (negative). Use O(n^2) naive implementation per design doc. For each date i, compute cummax of window[0:i+1], then dd = (window - cummax) / cummax, result[i] = dd.min(). First 2 days -> NaN. Apply per-column via `.apply(_rolling_max_dd, axis=0)`.
    - All three return FactorOutput with kind="score"

    **4. factors/composite.py:**
    - `CompositeFactor(BaseFactor)` with `__init__(self, factors: List[BaseFactor], weights: Optional[List[float]] = None, name: str = "composite")`
    - Assert all factors have kind=="score" in __init__
    - `compute()`: For each factor, compute output, then cross-sectional Z-Score per date row:
      - `mean = scores.mean(axis=1)` (per date, across funds)
      - `std = scores.std(axis=1, ddof=1)`
      - `z = scores.sub(mean, axis=0).div(std.replace(0, np.nan), axis=0).fillna(0)`
      - Weighted sum: `combined += z * weight`
    - Returns FactorOutput with kind="score"

    **5. factors/__init__.py:**
    - Barrel export all public symbols

    **CRITICAL CONSTRAINTS:**
    - FactorOutput.values.shape MUST equal nav_panel.shape (same index and columns)
    - No I/O in factor classes (no DB reads, no API calls)
    - Use `math.sqrt()` not `np.sqrt()` for scalar sqrt (follow risk_metrics.py pattern)
    - MaxDrawdownFactor: document O(n^2) complexity in docstring, note future optimization path
  </action>
  <acceptance_criteria>
    - `grep -n "class BaseFactor" src/fund_screener/factors/base.py` returns matching line
    - `grep -n "class FactorOutput" src/fund_screener/factors/base.py` returns matching line
    - `grep -n "class MACrossFactor" src/fund_screener/factors/technical.py` returns matching line
    - `grep -n "class MomentumFactor" src/fund_screener/factors/quant.py` returns matching line
    - `grep -n "class SharpeFactor" src/fund_screener/factors/quant.py` returns matching line
    - `grep -n "class MaxDrawdownFactor" src/fund_screener/factors/quant.py` returns matching line
    - `grep -n "class CompositeFactor" src/fund_screener/factors/composite.py` returns matching line
    - `python -c "from fund_screener.factors import BaseFactor, MACrossFactor, MomentumFactor, SharpeFactor, MaxDrawdownFactor, CompositeFactor; print('OK')"` outputs OK
    - `python -c "from fund_screener.factors.base import FactorOutput; import pandas as pd; f = FactorOutput(values=pd.DataFrame(), kind='signal', name='test'); print(f.kind)"` outputs signal
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrlonely/mrlonely/mrlonely-code/gitclone/analyze && python -c "from fund_screener.factors import BaseFactor, MACrossFactor, MomentumFactor, SharpeFactor, MaxDrawdownFactor, CompositeFactor; print('import OK')"</automated>
  </verify>
  <done>
    - factors/ package created with 5 files
    - All imports succeed with no errors
    - Each factor class inherits from BaseFactor
    - FactorOutput is frozen dataclass with correct fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Add load_nav_panel() to DataStore + pyproject.toml dependency</name>
  <files>
    src/fund_screener/storage.py
    pyproject.toml
  </files>
  <read_first>
    src/fund_screener/storage.py
    src/fund_screener/config.py
  </read_first>
  <action>
    **1. Add `load_nav_panel()` method to DataStore class in storage.py:**

    Insert after `get_stats()` method (around line 747), before the class ends:

    ```python
    def load_nav_panel(
        self,
        market: str,
        start_date: str,
        end_date: str,
        use_adj_nav: bool = False,
    ) -> pd.DataFrame:
        """
        Load NAV wide panel for backtest factor computation.

        Returns:
            DataFrame: index=date(DatetimeIndex), columns=fund_code, values=净值
        """
        nav_col = "adj_nav" if use_adj_nav else "nav"

        query = f"""
        SELECT f.code, nr.date, COALESCE(nr.{nav_col}, nr.nav) as effective_nav
        FROM nav_records nr
        JOIN funds f ON nr.fund_id = f.id
        WHERE f.market = ?
          AND nr.date BETWEEN ? AND ?
        ORDER BY nr.date ASC
        """

        df = pd.read_sql_query(query, self._conn, params=(market, start_date, end_date))

        if df.empty:
            return pd.DataFrame()

        df["date"] = pd.to_datetime(df["date"])
        panel = df.pivot(index="date", columns="code", values="effective_nav")
        panel = panel.ffill(limit=5)

        return panel
    ```

    **2. Add `load_benchmark()` method (adjacent to load_nav_panel):**

    ```python
    def load_benchmark(
        self,
        benchmark_code: str,
        start_date: str,
        end_date: str,
    ) -> pd.Series:
        """Load single benchmark index NAV series."""
        query = """
        SELECT nr.date, COALESCE(nr.adj_nav, nr.nav) as effective_nav
        FROM nav_records nr
        JOIN funds f ON nr.fund_id = f.id
        WHERE f.code = ?
          AND nr.date BETWEEN ? AND ?
        ORDER BY nr.date ASC
        """
        df = pd.read_sql_query(
            query, self._conn, params=(benchmark_code, start_date, end_date)
        )
        if df.empty:
            return pd.Series(dtype=float)
        df["date"] = pd.to_datetime(df["date"])
        return df.set_index("date")["effective_nav"]
    ```

    **3. Add vectorbt dependency to pyproject.toml:**

    In the `[project] dependencies` list, add `"vectorbt==0.26.0"` after `"uvicorn>=0.34.0"`.
    Also add `"numpy>=1.24"` if not already present (vectorbt needs it, but pandas already pulls it in).

    **CRITICAL:**
    - Use f-string for query (nav_col is a controlled literal, not user input)
    - ffill(limit=5) per D-02 decision
    - Return empty DataFrame if no data (not raise)
    - COALESCE(nr.adj_nav, nr.nav) fallback when adj_nav is NULL
  </action>
  <acceptance_criteria>
    - `grep -n "def load_nav_panel" src/fund_screener/storage.py` returns matching line
    - `grep -n "def load_benchmark" src/fund_screener/storage.py` returns matching line
    - `grep -n "vectorbt" pyproject.toml` returns matching line
    - `grep -n "ffill(limit=5)" src/fund_screener/storage.py` returns matching line
    - `grep -n "COALESCE(nr.\{nav_col\}, nr.nav)" src/fund_screener/storage.py` returns matching line (or equivalent SQL)
    - `python -c "from fund_screener.storage import DataStore; import inspect; assert 'load_nav_panel' in dir(DataStore); assert 'load_benchmark' in dir(DataStore); print('OK')"` outputs OK
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrlonely/mrlonely/mrlonely-code/gitclone/analyze && python -c "from fund_screener.storage import DataStore; print('load_nav_panel' in dir(DataStore) and 'load_benchmark' in dir(DataStore))"</automated>
  </verify>
  <done>
    - DataStore has load_nav_panel() and load_benchmark() methods
    - pyproject.toml includes "vectorbt==0.26.0" in dependencies
    - Method signatures match BACKTEST_DESIGN.md §5.2 exactly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write factor unit tests</name>
  <files>
    tests/test_factors.py
  </files>
  <read_first>
    tests/test_screener.py
    src/fund_screener/factors/base.py
    src/fund_screener/factors/technical.py
    src/fund_screener/factors/quant.py
    src/fund_screener/factors/composite.py
  </read_first>
  <behavior>
    - Test MACrossFactor: normal case (MA5 > MA10 for uptrend) -> True signals after period; first `long` days all False; NaN positions False
    - Test MACrossFactor: downtrend -> all False after period
    - Test MomentumFactor: positive momentum when price > MA; negative when price < MA
    - Test SharpeFactor: sufficient data returns finite float; zero volatility returns NaN
    - Test MaxDrawdownFactor: returns negative values; first 2 days NaN; larger drawdown = more negative
    - Test CompositeFactor: combines 2+ score factors; Z-Score handles std=0 (all same score) gracefully
    - Test CompositeFactor: __add__ syntax sugar (factor_a + factor_b)
    - Test FactorOutput shape invariant: output.values.shape == nav_panel.shape for all factors
  </behavior>
  <action>
    Create tests/test_factors.py with test classes for each factor.

    Use the same test structure as test_screener.py (class-based with pytest).

    Key test data patterns:
    - Create synthetic nav_panel with 3 funds x 30 days of data
    - Use `pd.date_range("2024-01-01", periods=30, freq="D")` as index
    - Fund A: linear uptrend (1.0 to 1.3)
    - Fund B: linear downtrend (1.3 to 1.0)
    - Fund C: mixed (first 15 up, then down)

    For shape invariant test:
    - Assert `output.values.shape == nav_panel.shape` for every factor
    - Assert `output.values.index.equals(nav_panel.index)`
    - Assert `list(output.values.columns) == list(nav_panel.columns)`

    For MACrossFactor boundary:
    - With short=5, long=10: first 10 rows must be all False
    - Introduce NaN in nav_panel (e.g., fund C first 5 days), assert those positions are False in signal

    For CompositeFactor:
    - Test with MomentumFactor + SharpeFactor (mock with small lookback for speed)
    - Test std=0 case: all funds have identical scores on a date -> Z-Score should be 0 (not NaN/inf)

    Run tests with: `uv run pytest tests/test_factors.py -v`
  </action>
  <acceptance_criteria>
    - `pytest tests/test_factors.py -v` passes all tests (minimum 12 test cases)
    - `grep -n "class Test" tests/test_factors.py` returns at least 6 test classes
    - `grep -n "def test_" tests/test_factors.py | wc -l` returns at least 12 test methods
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mrlonely/mrlonely/mrlonely-code/gitclone/analyze && uv run pytest tests/test_factors.py -v</automated>
  </verify>
  <done>
    - All tests pass (minimum 12 test cases)
    - Coverage: normal case + boundary case for each factor
    - Shape invariant verified for all factors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factor input -> output | nav_panel comes from storage.py (trusted), but NaN values are untrusted data quality issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | MaxDrawdownFactor.compute() | accept | O(n^2) complexity is a known performance issue documented in design. Mitigation: panel size limits in BacktestConfig, future numba optimization. Not a security issue. |
| T-03-02 | Information Disclosure | load_nav_panel() SQL query | mitigate | Query uses parameterized params (?, ?, ?). f-string only for column name selection (controlled literal "nav" or "adj_nav", not user input). |
| T-03-03 | Tampering | FactorOutput.values shape mismatch | mitigate | Unit tests enforce shape invariant. BacktestEngine in Plan 02 validates shape before passing to vectorbt. |
</threat_model>

<verification>
1. `python -c "from fund_screener.factors import ..."` — all imports succeed
2. `uv run pytest tests/test_factors.py -v` — all tests pass
3. `grep "def load_nav_panel" src/fund_screener/storage.py` — method exists
4. `grep "vectorbt==0.26.0" pyproject.toml` — dependency pinned
</verification>

<success_criteria>
- Factor layer package is importable and all classes instantiate correctly
- Each factor produces FactorOutput with values.shape == nav_panel.shape
- MACrossFactor correctly forces first `long` days to False and NaN positions to False
- CompositeFactor Z-Score handles std=0 without producing NaN/inf
- load_nav_panel() returns wide-format DataFrame with DatetimeIndex
- Unit tests cover normal + boundary cases for all 4 factor types
</success_criteria>

<output>
After completion, create `.planning/phases/03-backtest/03-01-SUMMARY.md`
</output>
