/**
 * Views — 业务视图组件 barrel export
 *
 * 所有页面统一从 @/components/views import，不直接引用子文件。
 */

/* ── Badge 类 ──────────────────────────────────────────── */
export { MarketBadge } from './MarketBadge'
export { ScoreBadge } from './ScoreBadge'
export { PurchaseStatusBadge } from './PurchaseStatusBadge'
export { MADiffIndicator } from './MADiffIndicator'

/* ── 卡片/列表 ─────────────────────────────────────────── */
export { StatsCard } from './StatsCard'
export { ScreeningResultItem } from './ScreeningResultItem'

/* ── 表格 ──────────────────────────────────────────────── */
export { FundTable } from './FundTable'

/* ── 详情页 ────────────────────────────────────────────── */
export { FundDetailHeader } from './FundDetailHeader'
export { HoldingsList } from './HoldingsList'
export { ChartContainer } from './ChartContainer'
