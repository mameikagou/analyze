## Git 工作流

- git 优先使用 rebase，少用 merge

## 前端约束

- 前端只使用 bun（`bun install` / `bun dev` / `bun build` 等），禁止 npm / yarn / pnpm

## 工程约束

- 你应该优先参考/gsd 的标准流程。不要自己瞎写拉屎。

## 语言约束

- 你干活用英文，但是最终写成文档和注释的时候要在最后一步翻译成中文。